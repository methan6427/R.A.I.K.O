name: shared_theme
description: Shared R.A.I.K.O theme package.
version: 0.1.0
publish_to: none

environment:
  sdk: ^3.10.7
  flutter: ">=3.35.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true